# Optional. If you terminate TLS/auth at a reverse proxy, you can leave this file unused.
# If you enable it, create TLS secrets at ./secrets/prometheus/tls.crt and tls.key and
# use bcrypt hashes (NOT cleartext passwords).

tls_server_config:
  cert_file: /run/secrets/prometheus_tls_cert
  key_file: /run/secrets/prometheus_tls_key

basic_auth_users:
  # admin: "$2y$10$REPLACE_WITH_BCRYPT_HASH"
