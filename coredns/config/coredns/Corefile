.:53 {
  errors
  log
  health
  ready

  prometheus :9153

  # Authoritative zones (example)
  file /etc/coredns/zones/db.example.internal example.internal
  file /etc/coredns/zones/db.home.arpa home.arpa

  # Default recursion forwarding (edit to your upstreams)
  forward . 1.1.1.1 9.9.9.9 {
    max_concurrent 1000
  }

  cache 30
  reload
}
