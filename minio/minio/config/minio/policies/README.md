# Policy files

Place MinIO policy JSON files here.

Example names:
- readonly.json
- readwrite.json

Then apply them in `minio-init` using `mc admin policy create` and attach to users/groups as needed.
