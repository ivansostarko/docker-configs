# Default settings sourced by entrypoint.sh (may be overridden by environment)
# Keep this file non-sensitive.

# mysqldump defaults (safe for InnoDB; includes routines/events/triggers)
DEFAULT_DUMP_ARGS="--single-transaction --quick --routines --events --triggers --hex-blob --set-gtid-purged=OFF"

# Filename format is handled by backup.sh
