# Listener ports
COTURN_PORT=3478
COTURN_PORT_SSL=5349

# Relay range (UDP). MUST match min-port/max-port in turnserver.conf
COTURN_RELAY_PORT_RANGE=49152-49200

# Identity / NAT
TURN_REALM=turn.example.com
TURN_EXTERNAL_IP=203.0.113.10

# Optional exporter port if you add one
COTURN_METRICS_PORT=9641
