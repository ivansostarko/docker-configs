appendonly yes
appendfilename "appendonly.aof"
dir /data

# Optional (recommended): require a password.
# If you enable this, your REDIS_URL must include it (redis://:password@infisical_redis:6379)
# requirepass YOUR_REDIS_PASSWORD
