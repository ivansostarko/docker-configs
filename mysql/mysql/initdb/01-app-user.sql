-- The database (MYSQL_DATABASE) and primary app user (MYSQL_USER + MYSQL_PASSWORD_FILE)
-- are created automatically by the official MySQL container entrypoint.
--
-- Use this file for additional grants once you know your schema boundaries.
-- Example (uncomment and adjust):
-- GRANT SELECT, INSERT, UPDATE, DELETE ON `appdb`.* TO 'appuser'@'%';

FLUSH PRIVILEGES;
