# Timezone
TZ=Europe/Zagreb

# MySQL exposure
MYSQL_BIND_IP=127.0.0.1
MYSQL_PORT=3306

# Bootstrap DB/user (only applied on first initialization)
MYSQL_DATABASE=appdb
MYSQL_USER=appuser

# Monitoring (profile: monitoring)
MYSQL_EXPORTER_BIND_IP=127.0.0.1
MYSQL_EXPORTER_PORT=9104

PROMETHEUS_BIND_IP=127.0.0.1
PROMETHEUS_PORT=9090

GRAFANA_BIND_IP=127.0.0.1
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin

# Ops tooling (profile: ops)
ADMINER_BIND_IP=127.0.0.1
ADMINER_PORT=8080
