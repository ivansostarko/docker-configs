# IMPORTANT: This vhost is reachable only inside Docker networks unless you publish 8081.
<VirtualHost *:8081>
  ServerName apache-status

  # Machine-readable status output is at /server-status?auto (what the exporter scrapes)
  <Location "/server-status">
    SetHandler server-status
    Require all granted
  </Location>
</VirtualHost>
