# Exposed host port for the exporter (container listens on 9216).
MONGODB_EXPORTER_PORT=9216

# Optional extra arguments passed to mongodb_exporter.
# Examples:
#   MONGODB_EXPORTER_EXTRA_ARGS=--compatible-mode
#   MONGODB_EXPORTER_EXTRA_ARGS=--split-cluster
MONGODB_EXPORTER_EXTRA_ARGS=
