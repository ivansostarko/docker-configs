# Only required if you decide to publish the exporter port to the host.
# (By default, the compose file uses `expose:` and keeps metrics internal to the Docker network.)
WORDPRESS_DB_EXPORTER_PORT=9104
