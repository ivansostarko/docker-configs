server {
  listen 80;
  server_name _;

  location / {
    return 200 "nginx ok\n";
    add_header Content-Type text/plain;
  }

  location = /healthz {
    return 200 "ok\n";
    add_header Content-Type text/plain;
  }
}
