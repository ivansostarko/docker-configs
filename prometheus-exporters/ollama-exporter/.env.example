# ------------------------------------------------------------------------------
# Ollama Exporter (lucabecker42/ollama-exporter)
# ------------------------------------------------------------------------------

# Exporter listen port (inside container and, if published, on host)
OLLAMA_EXPORTER_PORT=8000

# Bind address for published host port (recommended: localhost only)
OLLAMA_EXPORTER_BIND=127.0.0.1

# Poll interval & timeout (seconds)
OLLAMA_EXPORTER_INTERVAL=30
OLLAMA_EXPORTER_API_TIMEOUT=30

# Where the exporter should reach Ollama FROM INSIDE the container
# - If Ollama is another container on the same Docker network:  ollama:11434
# - If Ollama runs on the Docker host:                         host.docker.internal:11434
OLLAMA_HOST=ollama:11434
