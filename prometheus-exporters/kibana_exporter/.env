# ---------- Networking ----------
# Must already exist (external: true). Use the same network Kibana is on.
MONITORING_NETWORK_NAME=asterix_network

# ---------- Kibana target ----------
# IMPORTANT: This URL must be reachable from inside the Docker network.
# Example: http://kibana:5601
KIBANA_URI=http://kibana:5601

# If Kibana is protected (typical), set these.
KIBANA_USERNAME=elastic
# Password is read from Docker secret file: ./secrets/kibana_password.txt

# Set true if Kibana uses self-signed TLS and you accept the risk.
KIBANA_SKIP_TLS=false

# Upstream exporter tag should match your Kibana compatibility.
KIBANA_EXPORTER_UPSTREAM_TAG=v8.7.x.1

# ---------- Exporter listen ----------
KIBANA_EXPORTER_PORT=9684
KIBANA_EXPORTER_TELEMETRY_PATH=/metrics

# Wait for Kibana to become responsive before exporter starts.
KIBANA_EXPORTER_WAIT=true
