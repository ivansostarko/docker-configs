## Allow remote logins for non-guest users; keep guest local-only.
loopback_users.guest = true

## Optional: tune listener defaults (leave commented if you prefer image defaults)
# listeners.tcp.default = 5672
# management.tcp.port = 15672
# prometheus.tcp.port = 15692
