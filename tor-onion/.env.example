# ---- Tor metrics (internal-only) ----
TOR_METRICS_PORT=9035

# Must match docker-compose.yml monitoring subnet.
# Use a fixed IP so Tor can strictly allow only Prometheus via MetricsPortPolicy.
PROMETHEUS_STATIC_IP=172.19.0.100

# ---- Grafana ----
GRAFANA_ADMIN_USER=admin
