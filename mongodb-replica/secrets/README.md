# Secrets

These files are intentionally **not** committed to VCS.

Create/replace the following files:

- `mongo_root_password.txt`
- `mongo_app_password.txt`
- `mongo_exporter_password.txt`
- `mongo_keyfile.txt` (replica set internal authentication)
- `grafana_admin_password.txt`

See the main README for generation commands.
