# Databack runtime configuration
# Upstream expects DB_URL, REDIS_URL, SENTRY_DSN, ENV, WORKER, SECRET_KEY.
# Reference: https://github.com/long2ice/databack#configuration

# Core
ENV=production
DEBUG=False
WORKER=True

# Databack metadata database (this stack)
# NOTE: Password is not stored here; it is assembled into DB_URL using the postgres secret.
# For simplicity, we set DB_URL directly. If you want full "no-secrets-in-env" you must build a small wrapper.
DB_URL=postgres://databack:CHANGE_ME@postgres:5432/databack

# Rearq / Redis
REDIS_URL=redis://redis:6379/0

# Optional Sentry (leave empty if unused)
SENTRY_DSN=

# Required by the Awesome Docker Compose sample; Databack also uses it. 
# Set this to a strong random value (see README). 
SECRET_KEY=CHANGE_ME
