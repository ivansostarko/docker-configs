# ./config/postal-mariadb/my.cnf
# Minimal, safe defaults. Tune only after you measure.
[mysqld]
max_connections=200
innodb_buffer_pool_size=512M
innodb_log_file_size=256M
innodb_flush_log_at_trx_commit=2
sync_binlog=0
