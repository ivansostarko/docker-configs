# MailHog ports (host side)
MAILHOG_SMTP_PORT=1025
MAILHOG_UI_PORT=8025

# Identity
MAILHOG_HOSTNAME=mailhog.local

# Storage: memory | maildir | mongodb
MAILHOG_STORAGE=memory

# Optional: only if you actually need these
MAILHOG_CORS_ORIGIN=
MAILHOG_UI_WEB_PATH=
