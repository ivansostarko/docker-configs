# ------------------------
# Ollama host binding
# ------------------------
# Safer default: local-only access from the host machine
OLLAMA_BIND=127.0.0.1
OLLAMA_PORT=11434

# ------------------------
# Runtime behavior
# ------------------------
OLLAMA_KEEP_ALIVE=5m

# If you access from browsers/UIs, set allowed origins explicitly (comma-separated patterns)
# OLLAMA_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ------------------------
# Hard caps (tune for your host)
# ------------------------
OLLAMA_MEM_LIMIT=8g
OLLAMA_PIDS_LIMIT=4096

# ------------------------
# GPU (NVIDIA)
# ------------------------
# Set 0 or remove the deploy.devices block if you're CPU-only.
OLLAMA_GPU_COUNT=1

# ------------------------
# Metrics exporter (enable with: docker compose --profile metrics up -d)
# ------------------------
OLLAMA_EXPORTER_BIND=127.0.0.1
OLLAMA_EXPORTER_PORT=8000

# Optional network proxy (model pulls)
# HTTPS_PROXY=
# NO_PROXY=
